# Publish job: one Atomic Flatpak Release after automatic X3 proof passes.
#
# Signing and Pages publication run only when the automatic x3 workflow
# (triggered by successful validate on main) concludes with success for
# main. Each architecture rebuilds
# its own OSTree repo from the validated commit; the release job merges
# both into one current-only site repo, signs it, prunes history, tags the
# release for audit and rollback, and deploys to the Flatpak Repository on
# Pages. Chain: validate on main -> x3 -> publish fits the three
# workflow_run level limit. Checkouts use the triggering run head SHA,
# never implicit default-branch bytes. Manual or pull request runs never
# publish. A failed release publishes nothing; a separate rollback job redeploys
# the tagged prior release with its own Pages upload, so the release job
# never uploads the Pages artifact twice. Missing GPG_KEY fails closed.
# Secrets are named, never embedded.

name: publish

on:
  workflow_run:
    workflows: [x3]
    types: [completed]
    branches: [main]

permissions:
  contents: read

concurrency:
  group: publish
  cancel-in-progress: false

jobs:
  build-x86_64:
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'workflow_run' && github.event.workflow_run.head_branch == 'main' }}
    runs-on: ubuntu-24.04
    steps:
      - name: Check out the X3-proven commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Build the x86_64 OSTree repo
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder
          sudo flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          sudo flatpak install -y flathub org.kde.Platform//6.11 org.kde.Sdk//6.11
          flatpak-builder --force-clean --repo=repo build-dir io.github.viniciosrab.GrokBot.yml

      - name: Upload the x86_64 repo
        uses: actions/upload-artifact@v4
        with:
          name: repo-x86_64
          path: repo

  build-aarch64:
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'workflow_run' && github.event.workflow_run.head_branch == 'main' }}
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Check out the X3-proven commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Build the aarch64 OSTree repo
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y flatpak flatpak-builder
          sudo flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
          sudo flatpak install -y flathub org.kde.Platform//6.11 org.kde.Sdk//6.11
          flatpak-builder --force-clean --repo=repo build-dir io.github.viniciosrab.GrokBot.yml

      - name: Upload the aarch64 repo
        uses: actions/upload-artifact@v4
        with:
          name: repo-aarch64
          path: repo

  release:
    needs: [build-x86_64, build-aarch64]
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'workflow_run' && github.event.workflow_run.head_branch == 'main' }}
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      pages: write
      id-token: write
    environment:
      name: github-pages
    steps:
      - name: Check out the X3-proven commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0
          fetch-tags: true

      - name: Fail closed on missing secrets
        env:
          GPG_KEY: ${{ secrets.GPG_KEY }}
        run: |
          set -euo pipefail
          if [ -z "${GPG_KEY}" ]; then
            echo "GPG_KEY is not configured; refusing to sign a release" >&2
            exit 1
          fi

      - name: Collect both architecture repos
        uses: actions/download-artifact@v4
        with:
          path: arch-repos

      - name: Assemble one current-only signed site repo
        env:
          GPG_KEY: ${{ secrets.GPG_KEY }}
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y flatpak ostree
          rm -rf site
          ostree init --repo=site --mode=archive
          for arch in x86_64 aarch64; do
            dir="arch-repos/repo-${arch}"
            test -d "${dir}" || { echo "missing arch repo ${dir}" >&2; exit 1; }
            test -f "${dir}/config" || { echo "malformed arch repo ${dir}: missing config" >&2; exit 1; }
            test -d "${dir}/refs/heads" || { echo "malformed arch repo ${dir}: missing refs/heads" >&2; exit 1; }
            mkdir -p "${dir}/refs/remotes"
          done
          ostree pull-local --repo=site arch-repos/repo-x86_64
          ostree pull-local --repo=site arch-repos/repo-aarch64
          printf '%s' "${GPG_KEY}" | gpg --import
          KEY_ID="$(gpg --list-secret-keys --with-colons | awk -F: '/^sec:/ {print $5; exit}')"
          test -n "${KEY_ID}" || { echo "no GPG signing key available" >&2; exit 1; }
          flatpak build-update-repo --prune --gpg-sign="${KEY_ID}" site
          tar -czf site.tar.gz site

      - name: Tag the release for audit and rollback
        env:
          GH_TOKEN: ${{ github.token }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail
          VERSION="$(grep '^version:' data/pins.yml | awk '{print $2}')"
          test -n "${VERSION}" || { echo "missing pinned version" >&2; exit 1; }
          git fetch --tags origin || true
          LAST_R="$(git tag --list "flatpak/${VERSION}-r*" | sed -n 's/^flatpak\/.*-r\([0-9][0-9]*\)$/\1/p' | sort -n | tail -n 1)"
          if [ -z "${LAST_R}" ]; then
            N=1
          else
            N=$((LAST_R + 1))
          fi
          TAG="flatpak/${VERSION}-r${N}"
          echo "RELEASE_R=${N}" >> "${GITHUB_ENV}"
          git tag "${TAG}" "${HEAD_SHA}"
          git push origin "${TAG}"
          if gh release view "${TAG}" >/dev/null 2>&1; then
            gh release upload "${TAG}" site.tar.gz --clobber
          else
            gh release create "${TAG}" site.tar.gz \
              --title "Atomic Flatpak Release ${VERSION} -r${N} (x86_64 + aarch64)" \
              --notes "Current-only OSTree for x86_64 and aarch64. Rollback target on failure."
          fi

      - name: Deploy the Flatpak Repository to Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Publish to the Pages environment
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Mark the deployed release
        env:
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail
          VERSION="$(grep '^version:' data/pins.yml | awk '{print $2}')"
          test -n "${VERSION}" || { echo "missing pinned version" >&2; exit 1; }
          test -n "${RELEASE_R:-}" || { echo "missing release revision" >&2; exit 1; }
          MARKER="flatpak/deployed-${VERSION}-r${RELEASE_R}"
          git fetch --tags origin || true
          if git rev-parse "${MARKER}" >/dev/null 2>&1; then
            MARKER_SHA="$(git rev-list -n 1 "${MARKER}")"
            if [ "${MARKER_SHA}" = "${HEAD_SHA}" ]; then
              echo "${MARKER} already points at ${HEAD_SHA}; skipping create"
            else
              echo "${MARKER} points at ${MARKER_SHA}, not the deployed ${HEAD_SHA}; refusing to move deployed marker" >&2
              exit 1
            fi
          else
            git tag "${MARKER}" "${HEAD_SHA}"
            git push origin "${MARKER}"
          fi

  rollback:
    needs: [release]
    if: ${{ failure() && needs.release.result == 'failure' }}
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
    steps:
      - name: Check out the repository with tags
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0
          fetch-tags: true

      - name: Recover the tagged prior release on failure
        id: recover
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git fetch --tags origin || true
          PREV_TAG="$(git tag --list 'flatpak/deployed-*-r*' --sort=-v:refname | sed -n '1p')"
          if [ -z "${PREV_TAG}" ]; then
            echo "first release: no prior deployment to recover; nothing to roll back"
            echo "recovered=false" >> "${GITHUB_OUTPUT}"
            exit 0
          fi
          MARKER_SHA="$(git rev-list -n 1 "${PREV_TAG}")"
          REL_TAG="$(git tag --points-at "${MARKER_SHA}" --list 'flatpak/*-r*' | sort -V | tail -n 1)"
          test -n "${REL_TAG}" || { echo "no release tag points at ${PREV_TAG}" >&2; exit 1; }
          rm -f site.tar.gz
          rm -rf site
          gh release download "${REL_TAG}" --pattern 'site.tar.gz'
          tar -xzf site.tar.gz
          test -d site || { echo "recovered ${REL_TAG} has no site directory" >&2; exit 1; }
          echo "recovered=true" >> "${GITHUB_OUTPUT}"
          echo "recovered ${REL_TAG}; uploading it to Pages for redeploy"

      - name: Upload the recovered site for rollback
        if: steps.recover.outputs.recovered == 'true'
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Publish the recovered site to Pages
        if: steps.recover.outputs.recovered == 'true'
        uses: actions/deploy-pages@v4
